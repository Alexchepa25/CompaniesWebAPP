import React from 'react';
import './Input.css';

export const Input = (props) => {
       
    return (
        <section>
            <input 
                {...props}
            />
        </section>
    )
}
